<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="course.examples.ui.basiccolorpalette">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MaterialTheme">
        <activity
            android:name="course.examples.ui.basiccolorpalette.DisplayColorNames"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="course.examples.ui.basiccolorpalette.DisplayColorActivity"
            android:label="@string/app_name" />
        <activity
            android:name="course.examples.ui.basiccolorpalette.DisplaySingleColorActivity"
            android:label="@string/app_name" />
    </application>

</manifest>
